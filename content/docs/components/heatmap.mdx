---
title: Heatmap
description: Grids of cells coloured by value, for patterns over time and across groups.
---

Hover a cell to read it in the line under the grid. With the keyboard, Tab into the grid and use the arrow keys, Home and End to move between cells; screen readers announce each cell with its row, column and value.

### Weekday by Hour

When visitors arrive, by day of the week and hour of the day.

<ComponentPreview name='heatmap-01' />

### Cohort Retention

The share of each weekly signup cohort still active in each week after signup.

<ComponentPreview name='heatmap-02' />

### Calendar

A year of daily values. On narrow screens the grid scrolls sideways and starts at the most recent weeks.

<ComponentPreview name='heatmap-03' />

### Hot Spots

p95 latency by endpoint over the last 24 hours, darker where it's slower.

<ComponentPreview name='heatmap-04' />
