---
title: Stat Group
description: A row of related metrics in one card, each with its change vs the previous period.
---

### Plain Row

Three metrics side by side, each with its change and previous value. The row keeps its columns on narrow screens.

<ComponentPreview name='stat-group-01' previewClassName='max-w-2xl' />

### With Sparklines

Each metric with a 14-day sparkline. On narrow screens the sparkline moves beside the value.

<ComponentPreview name='stat-group-02' previewClassName='max-w-4xl' />

### Metric Tabs

Select a metric to chart it against the previous period. The tabs work with the arrow keys.

<ComponentPreview name='stat-group-03' previewClassName='max-w-4xl' />

### Compact Strip

Five metrics in a slim row that wraps to two columns on narrow screens.

<ComponentPreview name='stat-group-04' previewClassName='max-w-4xl' />

### Responsive Grid

Four columns, then a 2×2 grid, then a single stack as the card narrows.

<ComponentPreview name='stat-group-05' previewClassName='max-w-5xl' />
